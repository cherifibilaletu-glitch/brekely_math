# Copy this file to server/.env and fill in your OWN values.
# NEVER commit the real .env file. Rotate any password that was shared in chat.

# Your MongoDB Atlas connection string (include the password and database name)
MONGODB_URI="mongodb+srv://USERNAME:PASSWORD@cluster0.njnswn0.mongodb.net/brekely_math?retryWrites=true&w=majority"

# Port the API listens on
PORT=4000

# Allowed origin for the frontend (use your GitHub Pages URL in production)
CORS_ORIGIN="*"
